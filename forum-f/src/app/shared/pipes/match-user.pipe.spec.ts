import { MatchUserPipe } from './match-user.pipe';

describe('MatchUserPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchUserPipe();
    expect(pipe).toBeTruthy();
  });
});
